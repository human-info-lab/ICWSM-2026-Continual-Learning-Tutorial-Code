# ICWSM-2026-Continual-Learning-Tutorial-Code

## How to Evaluate?

run `evaluate.py`. For example to run all evaluations for the `roberta-base` model, you can run:

```bash
uv run -m src.evaluate
```